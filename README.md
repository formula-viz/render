# render

This is the repository which will use headless blender, importing the track, car, and camera data and actually building the animation video.

Flow:

- First import the track, build the main track portion and the curbs and color the sections accordingly.
- Second, import the cars, create actually objects of them and color accordingly.
- Finally, import the camera
- Render the relevant frames.
